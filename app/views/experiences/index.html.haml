%h1 Listing experiences
- if current_user
  = link_to 'New Experience', new_experience_path

%div{ style: 'clear: both; hight: 100%' }
  - @experiences.each do |experience|
    = render partial: "card", locals: { card: experience, is_left: true }
